services:
  - type: web
    name: nightazimuth-api
    runtime: python
    plan: free
    buildCommand: pip install ".[api]"
    startCommand: uvicorn nightazimuth.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
    autoDeployTrigger: commit
